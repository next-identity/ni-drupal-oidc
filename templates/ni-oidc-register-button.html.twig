{#
/**
 * @file
 * Default theme implementation for a Next Identity register button.
 *
 * Available variables:
 * - text: The button text.
 * - url: The URL to redirect to.
 * - icon_path: Path to the Next Identity icon.
 *
 * @ingroup themeable
 */
#}
<a href="{{ url }}" class="ni-oidc-register-button button">
  <span>{{ text }}</span>
</a> 