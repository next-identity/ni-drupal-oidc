{#
/**
 * @file
 * Default theme implementation for a Next Identity logout button.
 *
 * Available variables:
 * - text: The button text.
 * - url: The URL to redirect to.
 *
 * @ingroup themeable
 */
#}
<a href="{{ url }}" class="ni-oidc-logout-button button button--secondary">
  <span>{{ text }}</span>
</a> 