{#
/**
 * @file
 * Default theme implementation for a Next Identity profile button.
 *
 * Available variables:
 * - text: The button text.
 * - url: The URL to redirect to.
 *
 * @ingroup themeable
 */
#}
<a href="{{ url }}" class="ni-oidc-profile-button button button--primary">
  <span>{{ text }}</span>
</a> 